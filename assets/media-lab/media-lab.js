(() => {
  "use strict";
  const escapeHtml = value => String(value ?? "").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
  const formatValue = value => value === null ? "null" : typeof value === "boolean" ? String(value) : String(value);
  const setupTheme = () => {const button=document.getElementById("themeToggle");if(!button)return;const saved=localStorage.getItem("ai-sharing-theme");if(saved)document.documentElement.dataset.theme=saved;button.addEventListener("click",()=>{const next=document.documentElement.dataset.theme==="dark"?"light":"dark";document.documentElement.dataset.theme=next;localStorage.setItem("ai-sharing-theme",next);});};
  async function loadJson(path){const response=await fetch(path,{cache:"no-store"});if(!response.ok)throw new Error(`无法读取 ${path}`);return response.json();}
  async function renderIndex(){const app=document.getElementById("mediaLabApp");const data=await loadJson("../data/media-lab/cases.json");app.innerHTML=`<header class="topbar"><div class="container nav"><a class="brand" href="../">AI Sharing · AI Media Lab</a><button class="theme" id="themeToggle">明暗</button></div></header><main><section class="hero"><div class="container"><span class="eyebrow">PUBLIC AI MEDIA CASES</span><h1>AI Media Lab</h1><p>公开展示经过授权、脱敏与压缩的 AI 图像和视频实验案例。</p></div></section><section><div class="container case-list">${data.cases.map(item=>`<a class="panel case-link" href="${escapeHtml(item.href)}"><img src="${escapeHtml(item.poster)}" alt="${escapeHtml(item.title)}视频封面"><div class="case-copy"><h2>${escapeHtml(item.title)}</h2><p>${escapeHtml(item.summary)}</p></div></a>`).join("")}</div></section></main><footer><div class="container">AI Sharing · AI Media Lab</div></footer>`;setupTheme();}
  async function renderCase(){const app=document.getElementById("mediaLabApp");const d=await loadJson("../../../data/media-lab/issue-15.public.json");const parameterCards=Object.entries(d.parameters).map(([key,value])=>`<div class="card"><small>${escapeHtml(key)}</small><strong>${escapeHtml(formatValue(value))}</strong></div>`).join("");const reviews=d.evaluation.items.map(item=>`<article class="review-item"><h3>${escapeHtml(item.label)} · ${escapeHtml(item.status)}</h3><p>${escapeHtml(item.note)}</p></article>`).join("");app.innerHTML=`<header class="topbar"><div class="container nav"><a class="brand" href="../../">AI Sharing · AI Media Lab</a><button class="theme" id="themeToggle">明暗</button></div></header><main><section class="hero"><div class="container"><span class="eyebrow">IMAGE TO VIDEO · PUBLIC CASE</span><h1>${escapeHtml(d.title)}</h1><p>${escapeHtml(d.summary)}</p><div class="badges"><span class="badge">${escapeHtml(d.parameters.model)}</span><span class="badge">${escapeHtml(d.parameters.aspect_ratio)}</span><span class="badge">${d.parameters.duration_seconds} 秒</span><span class="badge pass">技术核验通过</span><span class="badge partial">评审部分完成</span></div></div></section><section><div class="container media-grid"><article class="panel media-card"><h2>参考图预览</h2><div class="stage"><img src="${escapeHtml(d.media.reference.src)}" alt="${escapeHtml(d.media.reference.alt)}"></div></article><article class="panel media-card"><h2>视频预览</h2><div class="stage"><video controls loop playsinline preload="metadata" poster="${escapeHtml(d.media.video.poster)}"><source src="${escapeHtml(d.media.video.src)}" type="video/mp4"></video></div></article></div></section><section><div class="container"><h2 class="section-title">完整正向 Prompt</h2><div class="panel prompt">${escapeHtml(d.prompt.positive)}</div></div></section><section><div class="container"><h2 class="section-title">完整负向 Prompt</h2><div class="panel prompt">${escapeHtml(d.prompt.negative)}</div><p class="note">${escapeHtml(d.prompt.enhance_note)}</p></div></section><section><div class="container"><h2 class="section-title">调用参数</h2><div class="grid">${parameterCards}</div></div></section><section><div class="container"><h2 class="section-title">比例决策</h2><div class="panel ratio"><p><strong>${escapeHtml(d.ratio_decision.input)}</strong> → <strong>${escapeHtml(d.ratio_decision.target)}</strong> → <strong>${escapeHtml(d.ratio_decision.actual)}</strong></p><p>${escapeHtml(d.ratio_decision.reason)}</p><p>${escapeHtml(d.ratio_decision.risk)}</p></div></div></section><section><div class="container"><h2 class="section-title">人工评审</h2><div class="panel review">${reviews}</div></div></section><section><div class="container note"><p>${escapeHtml(d.publication.source_note)}</p><p>${escapeHtml(d.publication.reuse_note)}</p></div></section></main><footer><div class="container">AI Sharing · Public AI Media Case</div></footer>`;setupTheme();}
  const page=document.body.dataset.page;Promise.resolve(page==="index"?renderIndex():renderCase()).catch(error=>{document.getElementById("mediaLabApp").textContent=error.message;});
})();
